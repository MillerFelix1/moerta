# 🚀 本地开发快速入门

## 📋 准备工作

### 1. 安装必需软件

- ✅ **Docker Desktop for Windows** 
  - 下载: https://www.docker.com/products/docker-desktop/
  - 安装后启动并确保状态为 "Running"

- ✅ **Git for Windows**（可选）
  - 下载: https://git-scm.com/download/win

---

## ⚡ 一键启动（最简单）

### 方法 1: 双击运行脚本 ⭐

1. 确保 **Docker Desktop** 正在运行
2. 双击 **`local-start.bat`**
3. 等待 3-5 分钟（首次需要下载和构建）
4. 看到提示后，浏览器打开 http://localhost

---

## 🎯 完整工作流程

### 开发流程图

```
┌─────────────────────────────────────────┐
│  1. 本地开发和测试                       │
│     ├─ 双击 local-start.bat (启动)      │
│     ├─ 修改代码                         │
│     ├─ 双击 local-update.bat (更新)     │
│     └─ 浏览器测试 http://localhost      │
├─────────────────────────────────────────┤
│  2. 确认无误后部署                       │
│     ├─ 双击 deploy-to-server.bat        │
│     ├─ 输入提交信息                      │
│     ├─ 自动推送到 GitHub                │
│     └─ SSH 到服务器完成部署             │
├─────────────────────────────────────────┤
│  3. 验证线上环境                         │
│     └─ 访问 https://shangyu.icu         │
└─────────────────────────────────────────┘
```

---

## 🛠️ 可用的脚本

| 脚本文件 | 功能 | 使用场景 |
|---------|------|---------|
| **local-start.bat** ⭐ | 启动本地开发环境 | 首次启动或完全重启 |
| **local-update.bat** | 快速更新本地服务 | 修改代码后刷新 |
| **local-stop.bat** | 停止本地服务 | 不用时停止释放资源 |
| **deploy-to-server.bat** 🚀 | 部署到生产服务器 | 测试通过后部署 |

---

## 📝 详细使用说明

### 🟢 首次启动

#### 步骤 1: 启动 Docker Desktop

- 双击桌面的 Docker Desktop 图标
- 等待状态变为 "Running"（绿色）

#### 步骤 2: 启动本地环境

```
双击 → local-start.bat
```

**会自动：**
1. ✅ 检查 Docker
2. ✅ 准备前端文件
3. ✅ 配置环境变量
4. ✅ 创建必要目录
5. ✅ 构建 Docker 镜像
6. ✅ 启动服务
7. ✅ 运行健康检查

**时间：** 首次 3-5 分钟，之后 30 秒

#### 步骤 3: 浏览器测试

打开浏览器访问：http://localhost

---

### 🔄 修改代码后更新

#### 场景 1: 只改了前端（HTML/CSS/JS）

```
双击 → local-update.bat → 选择 1
```

**时间：** 3 秒 ⚡

#### 场景 2: 只改了后端代码（Python）

```
双击 → local-update.bat → 选择 2
```

**时间：** 30-60 秒

#### 场景 3: 改了依赖或需要完全重建

```
双击 → local-update.bat → 选择 3
```

**时间：** 3-5 分钟

---

### 🚀 部署到服务器

#### 前提条件

- ✅ 本地测试通过
- ✅ 所有功能正常
- ✅ 没有错误

#### 步骤：

```
双击 → deploy-to-server.bat
```

**会引导你：**
1. 输入提交信息
2. 自动 Git 提交
3. 自动推送到 GitHub
4. 打开 SSH 窗口
5. 提示服务器部署命令

#### 在 SSH 窗口执行：

```bash
cd /var/www/shangyu.icu/yuanshi
git pull
bash smart-update.sh
bash check-system.sh
```

#### 验证部署：

访问 https://shangyu.icu

---

### 🛑 停止本地服务

```
双击 → local-stop.bat
```

**会停止并清理容器，释放系统资源。**

---

## 🧪 测试清单

### 基础功能测试

打开 http://localhost，检查：

- [ ] 页面正常加载（白色背景）
- [ ] 可以新建对话
- [ ] 可以发送消息
- [ ] 能收到 AI 回复（流式显示）
- [ ] 会话标题自动生成
- [ ] 可以切换/删除/重命名会话
- [ ] 代码高亮正常
- [ ] 复制代码按钮有效

### 移动端测试

按 F12 打开开发者工具 → 切换到手机模式：

- [ ] 布局适配良好
- [ ] 右下角有菜单按钮
- [ ] 点击菜单可展开侧边栏
- [ ] 输入框大小合适

### API 测试

打开浏览器控制台（F12），检查：

- [ ] 没有红色错误信息
- [ ] Network 标签中 API 调用都是 200
- [ ] 流式响应正常

---

## 📊 常用命令

### 在项目目录打开 PowerShell：

```powershell
# 查看服务状态
docker compose ps

# 查看实时日志
docker compose logs -f

# 重启服务
docker compose restart

# 查看后端日志
docker compose logs -f backend

# 查看 Nginx 日志
docker compose logs -f nginx
```

---

## 🐛 常见问题

### Q: 端口 80 被占用

**错误：** `Bind for 0.0.0.0:80 failed: port is already allocated`

**解决方案 1：** 停止占用端口的程序

```powershell
# 查找占用端口的进程
netstat -ano | findstr :80

# 停止 IIS（如果在运行）
net stop was /y
```

**解决方案 2：** 修改端口

编辑 `docker-compose.yml`：
```yaml
ports:
  - "8080:80"  # 改为 8080
```

然后访问 http://localhost:8080

---

### Q: Docker Desktop 无法启动

**解决：**
1. 确保 WSL 2 已安装
2. Windows 功能中启用"虚拟机平台"和"适用于 Linux 的 Windows 子系统"
3. 重启电脑
4. 重新启动 Docker Desktop

---

### Q: 构建很慢

**原因：** 首次构建需要下载基础镜像和依赖

**正常情况：**
- 首次：3-5 分钟
- 之后：30-60 秒（使用缓存）

**加速方法：**
- 等待首次构建完成
- 之后会使用缓存，快很多

---

### Q: 前端修改没生效

**解决：**
1. 确认修改了 `frontend/index-optimized.html`（不是 `index.html`）
2. 运行 `local-update.bat` 选择 1
3. 清除浏览器缓存（Ctrl+Shift+Delete）
4. 刷新页面（Ctrl+F5）

---

### Q: 后端 API 报错

**检查步骤：**

```powershell
# 1. 查看后端日志
docker compose logs backend

# 2. 检查环境变量
type backend\.env

# 3. 进入容器调试
docker compose exec backend bash
python -c "from dashscope import Application; print('OK')"
```

---

## 📚 相关文档

- 📖 **[LOCAL_DEVELOPMENT.md](LOCAL_DEVELOPMENT.md)** - 详细开发指南
- 🚀 **[QUICK_DEPLOY.md](QUICK_DEPLOY.md)** - 服务器部署
- 🔧 **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - 问题排查
- 🔄 **[WHY_REBUILD.md](WHY_REBUILD.md)** - 更新策略

---

## 🎯 典型的一天

### 早上开始工作

```
1. 打开 Docker Desktop
2. 双击 local-start.bat
3. 等待 30 秒
4. 开始开发 ☕
```

### 修改代码

```
1. VSCode 编辑文件
2. 双击 local-update.bat
3. 选择对应的更新类型
4. 浏览器刷新测试
```

### 测试通过后部署

```
1. 双击 deploy-to-server.bat
2. 输入提交信息
3. 等待自动推送
4. SSH 窗口执行部署命令
5. 验证线上环境 ✅
```

### 下班

```
1. 双击 local-stop.bat
2. 关闭 Docker Desktop（可选）
```

---

## ✅ 快速参考卡

```
┌─────────────────────────────────────────┐
│  我想...              应该...            │
├─────────────────────────────────────────┤
│  启动本地环境        local-start.bat     │
│  更新前端            local-update.bat→1  │
│  更新后端            local-update.bat→2  │
│  完全重建            local-update.bat→3  │
│  停止服务            local-stop.bat      │
│  部署到服务器        deploy-to-server.bat│
│  查看日志            docker compose logs -f│
│  查看状态            docker compose ps   │
└─────────────────────────────────────────┘
```

---

<div align="center">

**本地开发 → 测试验证 → 一键部署**

**简单、快速、专业** 🚀

有问题？查看 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

</div>

